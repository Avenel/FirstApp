<h2>OZB Konten</h2>



<!-- :ktoNr, :eeKtoNr,:zeNr, :zeAbDatum, :zeEndDatum, :zeBetrag, 
     :laufzeit, :zahlModus, :tilgRate, :ansparRate, :kduRate, :rduRate, :zeStatus -->

<h3 style="margin-top: 20px;">ZE-Konten</h3> 
<table style="margin: 10px auto;">
  <tr>
    <th>Konto-Nr.</th>
    <th>EEKonto-Nr.</th>
    <th>ZE-Nr.</th>
    <th>AbDatum</th>
    <th>EndDatum</th>
    <th>Betrag</th>
    <th>Laufzeit</th>
    <th>Zahlmodus</th>
    <th>Tilgungsrate</th>
    <th>Ansparrate</th>
    <th>Kdu-Rate</th>
    <th>Rdu-Rate</th>
    <th>Status</th>
  </tr>
  
  <tr>
    <% @ze_konten.each do |konto| %>
      <td><%= konto.ktoNr %></td>
      <td><%= konto.eeKtoNr %></td>
      <td><%= konto.zeNr %></td>
      <td><%= konto.zeAbDatum %></td>
      <td><%= konto.zeEndDatum %></td>
      <td><%= konto.zeBetrag %></td>
      <td><%= konto.laufzeit %></td>
      <td><%= konto.zahlModus %></td>
      <td><%= konto.tilgRate %></td>
      <td><%= konto.ansparRate %></td>
      <td><%= konto.kduRate %></td>
      <td><%= konto.rduRate %></td>
      <td><%= konto.zeStatus %></td>
    <% end %>
  </tr>  
</table>
<% if @ze_count < 4 then %>
  <%= link_to "Neues ZE-Konto hinzufügen", "/OZBPerson/"+params[:id]+"Konto/ZE/new" %>
<% end %>

<!-- :ktoNr, :bankId, :kreditlimit,  :id, :bankKtoNr, :blz, :bic, :iban, :bankName    -->

<h3 style="margin-top: 20px;">EE-Konten</h3> 
<table style="margin: 10px auto;">
  <tr>
    <th>Konto-Nr.</th>
    <th>Bank-Id</th>
    <th>Bank-Konto-Nr.</th>
    <th>Bankleitzahl</th>
    <th>Bankname</th>
    <th>Kreditlimit</th>
  </tr>
 
  <% @ee_konten.each do |konto| %>
    <tr>
      <td><%= konto.ktoNr %></td>
      <td><%= konto.bankId %></td>
      
      <% bankverbindung = Bankverbindung.where(:id => konto.bankId).first %>      
      <td><%= bankverbindung.bankKtoNr %></td>
      <td><%= bankverbindung.blz %></td>
      <td><%= bankverbindung.bankName %></td>
      <td><%= konto.kreditlimit %></td>
      <td><%= link_to "Editieren", "EE/"+konto.ktoNr.to_s %></td>
      <td><%= link_to "Löschen", "EE/"+konto.ktoNr.to_s+"/delete" %></td>
    </tr>
  <% end %>
</table>
<% if @ee_count < 3 then %>
  <%= link_to "Neues EE-Konto hinzufügen", "EE/new" %>
<% end %>
