<h1>EEKonto</h1>
<h2>EEKonto editieren</h2>

<%= form_for @ozb_konto, :url => { :action => "update" }, :html => {:id => "editForm", :method => "POST"}  do |f| %>

  <% if !@errors.nil? && @errors.any? %>
  <div id="error_explanation">
    <h2><%= @error_count %> Fehler <br /> Konnte kein Konto anlegen, weil:</h2>

    <ul>
      <% @errors.each do |error| %>
        <% if !error.nil? && error.any? %>
          <% error.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        <% end %>
      <% end %>
    </ul>
  </div>
  <% end %>

  <h2 style="margin-top:40px;">OZB-Konto</h2>
  <table>
    <tr>
      <td>Konto-Nr.:</td>
      <td><%= label_tag :ozbKtoNr, @ozb_konto.ktoNr %> <%= f.hidden_field :ktoNr%></td>
    </tr>
    <tr>
      <td>wSaldo:</td>
      <td><%= label_tag :wSaldo, @ozb_konto.wSaldo %></td>
    </tr>
    <tr>
      <td>pSaldo:</td>
      <td><%= label_tag :pSaldo, @ozb_konto.pSaldo %></td>
    </tr>
    <tr>
      <td>SaldoDatum:</td>
      <td><%= label_tag :saldoDatum, @ozb_konto.saldoDatum %></td>
    </tr>
  </table>
  
  
  <h2 style="margin-top:20px;">EE-Konto</h2>
  <table>
    <tr>
      <td>Konto-Nr.:</td>
      <td><%= label_tag :ozbKtoNr, @ee_konto.ktoNr %> <%= hidden_field_tag :eeKtoNr, @ee_konto.ktoNr%></td>
    </tr>
    <tr>
      <td>Kreditlimit:</td>
      <td><%= text_field_tag :kreditlimit, @ee_konto.kreditlimit %></td>
    </tr>
  </table>
  

<!-- Bankverbindung:  :id, :bankKtoNr, :blz, :bic, :iban, :bankName -->
  <h3 style="margin-top:20px;">Bankverbindung</h2>
  <table>
    <tr>
      <td>Bank-Konto-Nr.:</td>
      <td><%= text_field_tag :bankKtoNr, @bankverbindung.bankKtoNr %></td>
    </tr>
    <tr>
      <td>Bankname</td>
      <td><%= text_field_tag :bankName, @bankverbindung.bankName %></td>
    </tr>
    <tr>
      <td>Bankleitzahl:</td>
      <td><%= text_field_tag :blz, @bankverbindung.blz %></td>
    </tr>
    <tr>
      <td>BIC:</td>
      <td><%= text_field_tag :bic, @bankverbindung.bic %></td>
    </tr>
    <tr>
      <td>IBAN:</td>
      <td><%= text_field_tag :iban, @bankverbindung.iban %></td>
    </tr>
  </table>
  
  <br />
  
  <%= link_to "Abbrechen", "../", :class => "cancel_link" %>
  <%= link_to_function "Speichern", "this.form.submit()", :onclick => "$('#editForm').submit(); return false;", :class => "submit_ok"  %>  
<% end %>

<script>
  $(function() {
    $( "#saldoDatum" ).datepicker({
      dateFormat: 'yy-mm-dd'
    });
  });
</script>
