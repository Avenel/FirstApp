<h1>Kontenklassen</h1>


<table>
  <tr>
    <th>Name</th>
    <th>Prozent</th>
    <th>AbDatum</th>
    <th />
    <th />
  </tr>

  <% i = 0 %>
  <% @kontenklassen.each do |kkl| %>
    <% if i%2 == 0 then bg_color="white" else bg_color="lightgrey" end %>
    <tr style="background:<%= bg_color %>;">
      <td><%= kkl.kkl %></td>
      <td><%= kkl.prozent %></td>
      <td><%= kkl.kklAbDatum %></td>
      <% edit_link = "/kontenklasse/" + kkl.kkl.to_s %>
      <% delete_link = "/kontenklasse/" + kkl.kkl.to_s + "/delete" %>
      <% verlauf_link = "/kontenklasse/verlauf/" + kkl.kkl.to_s %>
      <td><%= link_to "Editieren", edit_link, :class => "edit_link" %></td>
      <td><%= link_to "Löschen", delete_link, :class => "delete_link", :confirm => "Sind Sie sicher?" %></td>
    </tr>
    <% i += 1 %>
  <% end %>
</table>

<!-- Seitennavigation -->
<div class="paginateBox" >
  <%= will_paginate @kontenklassen %> 
</div>

<br />

<table class="actions">
  <tr>
    <td><%= link_to "Zurück", "/Admin", :class => "cancel_link" %></td>
    <td><%= link_to "Neue Kontenklasse hinzufügen", "/kontenklasse/new", :class => "submit_ok" %></td>
  </tr>
</table>

