class Bankverbindung < ActiveRecord::Base
   
  set_table_name "Bankverbindung"

  set_primary_key :id

  attr_accessible :id, :pnr, :bankKtoNr, :blz, :bic, :iban, :bankName   

  belongs_to :Person
  has_one :EEKonto, :foreign_key => :bankId # Done, ungetestet

end
